import * as util from './utils';

// const files = require.context('@/utils', false, /\.ts$/);

// files.keys().forEach(file => {
//     const name = require('path').basename(file, '.ts');

//     console.log(files(file) || files(file).default);
// });

export { util };
